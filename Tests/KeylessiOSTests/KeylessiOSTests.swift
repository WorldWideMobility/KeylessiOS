import XCTest
@testable import KeylessiOS

final class KeylessiOSTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(KeylessiOS().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
